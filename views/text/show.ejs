<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes do Text</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Detalhes do Text</h1>
        
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= text.name %></h5>
                <p class="card-text"><strong>ID:</strong> <%= text.id %></p>
                <p class="card-text"><strong>Telefone:</strong> <%= text.telefone %></p>
                <p class="card-text"><strong>Email:</strong> <%= text.email %></p>
                
                <div class="mt-4">
                    <a href="/texts/<%= text.id %>/edit" class="btn btn-warning">Editar</a>
                    <form action="/texts/<%= text.id %>?_method=DELETE" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-danger">Excluir</button>
                    </form>
                    <a href="/texts" class="btn btn-secondary">Voltar</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>